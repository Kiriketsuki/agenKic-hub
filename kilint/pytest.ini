[pytest]
# Makes the "kilint" package importable as `import kilint` without an
# installed package or a src-layout: this file's directory (the kilint
# project root) is prepended to sys.path by pytest's rootdir insertion.
pythonpath = .
testpaths = tests
python_files = test_*.py
