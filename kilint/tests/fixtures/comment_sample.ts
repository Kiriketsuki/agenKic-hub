/**
 * This helper will facilitate the seamless integration of downstream modules.
 */
function helper(value: string): string {
  // ensure this branch always uses the correct format for every caller
  return value;
}
