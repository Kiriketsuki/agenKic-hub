# Codex CLI

Config target: `~/.codex/` (all platforms).

Expected contents once ported:

| File | Purpose |
|:---|:---|
| `config.toml.tmpl` | Model, approval policy, sandbox settings |
| `AGENTS.md` | Shared agent instructions |

Scaffold only for now. Source of truth: the author's Linux machine.
