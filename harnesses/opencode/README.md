# OpenCode

Config target: `~/.config/opencode/` (Linux/macOS), `%APPDATA%\opencode\` (Windows).

Expected contents once ported:

| File | Purpose |
|:---|:---|
| `opencode.json.tmpl` | Provider, model, and keybind settings |
| `AGENTS.md` | Shared agent instructions |

Scaffold only for now. Source of truth: the author's Linux machine.
