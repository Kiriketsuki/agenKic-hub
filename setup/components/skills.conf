# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
name=skills
desc=Symlink each skills/<name> directory into ~/.claude/skills/<name>
type=link_children
source=skills
target=~/.claude/skills
