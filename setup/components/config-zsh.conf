# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
# The rendered fragment is sourced from .zshrc by the user. See setup/README.md.
name=config-zsh
desc=Render config/zsh templates into ~/.config/zsh
type=install
source=config/zsh
target=~/.config/zsh
