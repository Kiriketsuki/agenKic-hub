# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
name=skills-ops
desc=Symlink each skills/ops/<name> directory into ~/.claude/skills/<name>
type=link_children
source=skills/ops
target=~/.claude/skills
