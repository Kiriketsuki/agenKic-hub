# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
# Scaffold only. Target is provisional until the harness README documents the real one.
name=harness-pi
desc=Install Pi harness files into ~/.config/pi (provisional target)
type=install
source=harnesses/pi
target=~/.config/pi
