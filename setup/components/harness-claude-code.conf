# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
# Scaffold only for now. Installer no-ops until real files land in harnesses/claude-code/.
name=harness-claude-code
desc=Install Claude Code harness files into ~/.claude
type=install
source=harnesses/claude-code
target=~/.claude
