# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
# Scaffold only for now. Installer no-ops until real files land in harnesses/codex/.
name=harness-codex
desc=Install Codex CLI harness files into ~/.codex
type=install
source=harnesses/codex
target=~/.codex
