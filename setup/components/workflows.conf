# Component manifest. Flat key=value, parsed by setup.sh and setup.ps1.
name=workflows
desc=Symlink each workflows/<name> entry into ~/.claude/workflows/<name>
type=link_children
source=workflows
target=~/.claude/workflows
